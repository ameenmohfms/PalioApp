"""Palio backend.

Palio screens, educates, supports, and refers. It never diagnoses.
Hard Rules (spec §4) govern every module in this package; the Safety
Sentinel (palio.safety) has veto power over all user-facing output.
"""

__version__ = "0.1.0"
